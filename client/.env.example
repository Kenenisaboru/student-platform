# Local dev: use Vite proxy (recommended — do not point at another app's port)
VITE_API_URL=/api

# Or call the API directly (must match server PORT in server/.env, default 5010):
# VITE_API_URL=http://localhost:5010/api
